using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class P_Sneak_Controller : MonoBehaviour
{
    /*
    AudioSource audio;
    P_Character_Move Sneak;
     void Start()
    {
        Sneak=FindAnyObjectByType<P_Character_Move>();
        audio=GetComponent<AudioSource>();
    }

    // Update is called once per frame
    void Update()
    {
        sneak();
        walksoundmanager();
    }

    void walksoundmanager()
    {
        if(Sneak.isWalking)
        {
            audio.Play();
        }else
        {
            audio.Pause();
        }

    }

    void sneak()
    {

        if(Sneak.isCrouched)
        {
            audio.volume = 0.5f;
        }else
        {
            audio.volume = 1.5f;
        }
    }
    */
}
